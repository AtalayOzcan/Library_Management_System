@using LibraryManagementSystem.Models
@using LibraryManagementSystem.Controllers
@model ICollection<Publisher>

<div class="container mt-5">
    <h2 class="mb-4 text-primary">Publishers</h2>
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th class="py-3">Publisher Name</th>
                            <th class="py-3 text-center" style="width: 180px;">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            @foreach (var x in Model)
                            {
                                <tr>
                                    <td class="fw-semibold align-middle">@x.PublisherName</td>

                                    <td class="text-center align-middle">
                                        <a asp-controller="Publishers" asp-action="Edit" asp-route-id="@x.PublisherId" class="btn btn-sm btn-outline-primary me-1">
                                            <i class="bi bi-pencil"></i> Edit
                                        </a>

                                        <a asp-controller="Publishers" asp-action="Delete" asp-route-id="@x.PublisherId" class="btn btn-sm btn-outline-danger" onclick="return confirm('Are you sure want to delete?');">
                                            <i class="bi bi-trash"></i> Delete
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="4" class="text-center py-5 text-muted">
                                    Publisher not found
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="d-flex justify-content-end mt-3">
        <a asp-controller="Publishers" asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-lg"></i> Add New Publisher
        </a>
    </div>
</div>